# Walrus Oparetor
if(n :=len([1,2,4,6]) ) > 3:
  print(f"list is too long,({n} elements,expected <= 3)")
# Math case
# Dictionary match and update operators and multiple context maneger
# Exception Handeling -
    # try,except
    # raise- custom error
    # try with else - try block successfully run hole else run korbe
    # try with finally- function e return korleo finally block cholbe #
# __name__ , __main__
# Global key word
# Enumerate function - for index,item in enumerate(l):
# List comprehensions
#
#
# Virtual Environment#

# lambda Function-
square = lambda x : x * x
print(square(7))

# join Method- a = ",".join(l)
#Format method - a = "{} is a good {}".format("partho","boy")
#Map (function_name,list_name)
#filter (function_name,list_name)
# reduce   (function_name,list_name)
